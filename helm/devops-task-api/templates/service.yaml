apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-devops-task-api

spec:
  type: {{ .Values.service.type }}

  selector:
    app: devops-task-api

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
